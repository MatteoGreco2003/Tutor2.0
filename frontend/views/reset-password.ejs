<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reimposta Password - Tutor 2.0</title>
    <link rel="stylesheet" href="css/registrazione.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css"
    />
  </head>

  <body>
    <!-- Sezione introduttiva -->
    <div class="intro">
      <h1>Tutor 2.0</h1>
      <p>Reimposta la tua password</p>
    </div>

    <!-- Container principale -->
    <div class="container" style="max-width: 500px; margin: 0 auto">
      <!-- Form Reset Password -->
      <div class="form-box login" style="width: 100%; transform: none">
        <form id="resetPasswordForm" novalidate>
          <h1>Reimposta Password</h1>
          <p style="font-size: 14px; color: #73738c; margin-bottom: 20px">
            Inserisci la tua nuova password
          </p>

          <div class="input-box password-input-box">
            <input
              type="password"
              id="newPassword"
              placeholder="Nuova Password"
              required
            />
            <i
              class="fa-solid fa-lock toggle-password"
              data-input="newPassword"
            ></i>
          </div>

          <div class="input-box password-input-box">
            <input
              type="password"
              id="confirmNewPassword"
              placeholder="Conferma Password"
              required
            />
            <i
              class="fa-solid fa-lock toggle-password"
              data-input="confirmNewPassword"
            ></i>
          </div>

          <div id="resetErrors" class="error-messages"></div>

          <div
            id="resetSuccess"
            class="success-message"
            style="
              display: none;
              background: #d4edda;
              border: 1px solid #c3e6cb;
              color: #155724;
              padding: 12px;
              border-radius: 8px;
              margin-bottom: 16px;
            "
          >
            ✅ Password reimpostata con successo! Reindirizzamento...
          </div>

          <button type="submit" class="btn">Reimposta Password</button>
        </form>
      </div>
    </div>

    <!-- Footer -->
    <div class="outro">
      <p>
        Powered by
        <a href="https://www.toptutor.it/" target="_blank">toptutor.it</a>
      </p>
    </div>

    <script src="js/reset-password.js"></script>
  </body>
</html>
